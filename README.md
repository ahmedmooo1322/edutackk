# EduTrack APK V6.5.1 Login Overlay Fix

Built from APK V6.5 teacher quizzes base.

Fix included:
- Removes the WhatsApp floating overlay from auth pages: Splash, Login, Register.
- Keeps WhatsApp floating button only after the user is inside the app.
- This prevents the red/tinted broken overlay on the login screen.
- Keeps debug/unsigned GitHub Actions build.

No backend changes are required for this patch.
